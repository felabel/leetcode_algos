var gcdOfStrings = function(str1, str2) {
    if (str1.includes(str2)){
        let sumStr2 = str2 + str2

        return str2
    } else {
        return ""
    }
};

console.log(gcdOfStrings("ABABAB", "ABAB"))